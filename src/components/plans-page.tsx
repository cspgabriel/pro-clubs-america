import Link from "next/link";
import { Check, Crown, Sparkles, Zap } from "lucide-react";
import { MobileNav } from "./mobile-nav";
import { PlatformHeader } from "./platform-header";
const plans = [{ name: "Free", icon: Zap, price: "R$ 0", text: "Para entrar na comunidade", features: ["Perfil de clube e jogador", "Histórico básico", "Desafios abertos"] }, { name: "Pro", icon: Sparkles, price: "Em breve", text: "Para clubes competitivos", features: ["Desafios destacados", "Histórico completo", "Exportação de dados", "Selo de confiabilidade"] }, { name: "VIP", icon: Crown, price: "Em breve", text: "Máxima visibilidade", features: ["Mercado prioritário", "Heatmaps e mapas de chute", "Alertas no Discord", "Destaque regional"] }];
export function PlansPage() { return <main className="app-shell"><PlatformHeader /><section className="plans-hero"><small>PLANOS DA COMUNIDADE</small><h1>Comece grátis.<br />Evolua com seu clube.</h1><p>Mockup comercial: cobranças ainda não estão habilitadas.</p></section><section className="plans-grid">{plans.map((plan) => { const Icon = plan.icon; return <article className={plan.name === "Pro" ? "featured" : ""} key={plan.name}><Icon /><small>{plan.name.toUpperCase()}</small><h2>{plan.price}</h2><p>{plan.text}</p><ul>{plan.features.map((feature) => <li key={feature}><Check />{feature}</li>)}</ul><Link href={plan.name === "Free" ? "/criar-conta" : "/conta"}>{plan.name === "Free" ? "Criar conta grátis" : "Entrar na lista"}</Link></article>; })}</section><MobileNav /></main>; }
