"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowLeft, CalendarDays, Clock3, ExternalLink, LockKeyhole, MapPin, ShieldCheck, Swords, Trophy, Users } from "lucide-react";
import type { MatchRecord } from "@/types/domain";
import type { CommunityMatchClub } from "@/lib/friendlies-data";
import type { FriendlyRequest } from "@/lib/friendlies";
import { watchFriendly } from "@/lib/community-service";
import { MobileNav } from "./mobile-nav";
import { PlatformHeader } from "./platform-header";
import { MatchLobby } from "./match-lobby";
export function MatchDetail({ id, official, clubs }: { id: string; official?: MatchRecord; clubs: CommunityMatchClub[] }) {
  const [request, setRequest] = useState<FriendlyRequest | null>(null); const [loaded, setLoaded] = useState(Boolean(official));
  useEffect(() => { if (official) return; return watchFriendly(id, (item) => { setRequest(item); setLoaded(true); }, () => setLoaded(true)); }, [id, official]);
  if (!official && loaded && !request) return <main className="app-shell"><PlatformHeader /><div className="match-not-found"><Swords /><h1>Partida não encontrada</h1><p>O convite não existe ou não está mais disponível.</p><Link href="/partidas">Voltar para partidas</Link></div><MobileNav /></main>;
  const hostId = official?.homeClubId ?? request?.hostClubId; const awayId = official?.awayClubId ?? request?.opponentClubId ?? request?.invitedClubId; const host = clubs.find((club) => club.id === hostId); const away = clubs.find((club) => club.id === awayId); const homeName = official?.homeClubName ?? request?.hostClubName ?? "Time da casa"; const awayName = official?.awayClubName ?? request?.opponentClubName ?? request?.invitedClubName ?? "Adversário em aberto";
  return <main className="app-shell match-detail-page"><PlatformHeader /><section className="match-detail-hero"><Link href="/partidas"><ArrowLeft /> Partidas</Link><small>{official ? "RESULTADO OFICIAL" : request?.status === "scheduled" ? "JOGO AGENDADO" : "DESAFIO DA COMUNIDADE"}</small><div className="match-detail-score"><section><span>{homeName.slice(0,2).toUpperCase()}</span><h1>{homeName}</h1><small>{host?.roster.length ?? 0} jogadores publicados</small></section><b>{official ? <>{official.homeScore}<i>×</i>{official.awayScore}</> : <><i>×</i></>}</b><section><span>{awayName.slice(0,2).toUpperCase()}</span><h1>{awayName}</h1><small>{away?.roster.length ?? 0} jogadores publicados</small></section></div><div className="match-detail-meta">{official ? <><span><CalendarDays />{new Date(official.playedAt).toLocaleString("pt-BR")}</span><span><Trophy />{official.competition}</span><a href={official.sourceUrl} target="_blank" rel="noreferrer">Fonte EA <ExternalLink /></a></> : <><span><CalendarDays />{request && new Date(`${request.date}T12:00:00`).toLocaleDateString("pt-BR")}</span><span><Clock3 />{request?.time}</span><span><MapPin />{request?.region}</span></>}</div></section><section className="match-detail-content"><article className="match-status-panel"><ShieldCheck /><div><small>VALIDAÇÃO DO PLACAR</small><h2>{official ? "Confirmado pela fonte pública" : request?.status === "waiting_ea" ? "Aguardando publicação oficial da EA" : "Placar manual bloqueado"}</h2><p>{official ? "Este resultado foi incorporado do histórico público do clube." : "Quando o confronto aparecer na aba Friendly da EA, o reconciliador poderá preencher placar e estatísticas automaticamente."}</p></div><LockKeyhole /></article><div className="match-squads">{[{ club: host, name: homeName }, { club: away, name: awayName }].map((team, index) => <article key={team.club?.id ?? index}><header><Users /><div><small>{index ? "VISITANTE" : "CASA"}</small><h2>{team.name}</h2></div><span>SR {team.club?.skillRating ?? "—"}</span></header><div>{team.club?.roster.length ? team.club.roster.slice(0,11).map((player, playerIndex) => <span key={`${player.name}-${playerIndex}`}><b>{String(playerIndex+1).padStart(2,"0")}</b><strong>{player.name}</strong><small>{player.position}</small></span>) : <p>Escalação ainda não publicada.</p>}</div></article>)}</div>{official?.players.length ? <article className="match-player-stats"><header><Trophy /><h2>Estatísticas da partida</h2></header>{official.players.map((player) => <div key={player.playerId}><strong>{player.playerName}</strong><span>{player.goals} G</span><span>{player.assists} A</span><span>{player.tacklesMade ?? 0} desarmes</span><b>{player.rating ?? "—"}</b></div>)}</article> : null}{request ? <MatchLobby match={request} /> : null}</section><MobileNav /></main>;
}
