import { PublicHome } from "@/components/public-home";

export const metadata = { title: { absolute: "Pro Clubs America | Encontre seu clube e entre no jogo" }, alternates: { canonical: "/" } };

export default function Home() {
  return <PublicHome />;
}
