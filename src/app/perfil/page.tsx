import { Suspense } from "react";
import { CommunityProfilePage } from "@/components/community-profile-page";

export default function Page() { return <Suspense><CommunityProfilePage /></Suspense>; }
